import { AnimatedFrame } from "../index.js";

export function temporal( // literally learn how interpolation work WHAT
    frames: AnimatedFrame[],
    speed: number // 0.5 (slowest) => 1 (neutral) => 1.5 (fastest)
) {
    
}